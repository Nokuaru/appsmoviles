import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Historia Clínica',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
